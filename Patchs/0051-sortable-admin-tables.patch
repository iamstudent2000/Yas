diff --git a/src/YasPortal.Web/Components/App.razor b/src/YasPortal.Web/Components/App.razor
index 6898845..1c636a6 100644
--- a/src/YasPortal.Web/Components/App.razor
+++ b/src/YasPortal.Web/Components/App.razor
@@ -21,7 +21,7 @@
     <link rel="stylesheet" href="css/components.css?v=20260912-1" />
     <link rel="stylesheet" href="css/notifications.css?v=20260828-1" />
     <link rel="stylesheet" href="css/admin/admin-core.css?v=20260912-1" />
-    <link rel="stylesheet" href="css/admin/admin-tables-and-lists.css?v=20260828-2" />
+    <link rel="stylesheet" href="css/admin/admin-tables-and-lists.css?v=20260913-1" />
     <link rel="stylesheet" href="css/admin/permission-checkboxes.css?v=20260828-1" />
     <link rel="stylesheet" href="css/admin/positions.css?v=20260912-1" />
     <link rel="stylesheet" href="css/rtl-utilities.css?v=20260828-1" />
diff --git a/src/YasPortal.Web/Components/Pages/Admin/AssignmentHistory.razor b/src/YasPortal.Web/Components/Pages/Admin/AssignmentHistory.razor
index cfb22ec..71c4c64 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/AssignmentHistory.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/AssignmentHistory.razor
@@ -1,6 +1,7 @@
 @page "/admin/assignment-history"
 @using Microsoft.AspNetCore.Authorization
 @using Microsoft.EntityFrameworkCore
+@using YasPortal.Domain.Organization
 @using YasPortal.Infrastructure.Persistence
 @using YasPortal.Web.Services
 @using YasPortal.Web.Components.Shared
@@ -82,11 +83,11 @@
                                 <table class="admin-table admin-table-bordered-rows">
                                     <thead>
                                         <tr>
-                                            <th>کارمند</th>
-                                            <th>سمت</th>
-                                            <th>شروع تخصیص</th>
-                                            <th>پایان تخصیص</th>
-                                            <th>وضعیت</th>
+                                            <SortableHeader Text="کارمند" Column="employee" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="سمت" Column="position" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="شروع تخصیص" Column="started" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="پایان تخصیص" Column="ended" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="وضعیت" Column="status" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
                                         </tr>
                                     </thead>
                                     <tbody>@foreach(var row in _rows){
@@ -139,6 +140,7 @@
     private string _employeeName = "", _positionName = "", _status = "all";
     private int _page = 1, _pageSize = 20, _total;
     private bool _loading;
+    private string _sortBy = "started"; private bool _sortDesc = true;
     protected override async Task OnInitializedAsync() => await LoadAsync();
     private async Task LoadAsync()
     {
@@ -153,12 +155,25 @@ x.EndedAt == null);
                 if (_status == "ended") query = query.Where(x => x.EndedAt != null); _total = await query.CountAsync(); var
                 totalPages = Math.Max(1, (int)Math.Ceiling(_total / (double)_pageSize)); if (_page > totalPages) _page = totalPages;
             _rows = await
-                            query.OrderByDescending(x => x.StartedAt ?? DateTime.MinValue).ThenByDescending(x => x.EndedAt ??
-            DateTime.MaxValue).Skip((_page - 1) * _pageSize).Take(_pageSize).Select(x => new
+                            OrderHistory(query).Skip((_page - 1) * _pageSize).Take(_pageSize).Select(x => new
                             HistoryRow(x.Id, x.Employee.FullName, x.Employee.Username, x.Position.Name, x.StartedAt, x.EndedAt)).ToListAsync();
         }
         finally { _loading = false; }
     }
+    private IQueryable<PositionAssignmentHistory> OrderHistory(IQueryable<PositionAssignmentHistory> query) => (_sortBy, _sortDesc) switch
+    {
+        ("employee", false) => query.OrderBy(x => x.Employee.FullName),
+        ("employee", true) => query.OrderByDescending(x => x.Employee.FullName),
+        ("position", false) => query.OrderBy(x => x.Position.Name),
+        ("position", true) => query.OrderByDescending(x => x.Position.Name),
+        ("started", false) => query.OrderBy(x => x.StartedAt ?? DateTime.MinValue),
+        ("started", true) => query.OrderByDescending(x => x.StartedAt ?? DateTime.MinValue),
+        ("ended", false) => query.OrderBy(x => x.EndedAt ?? DateTime.MaxValue),
+        ("ended", true) => query.OrderByDescending(x => x.EndedAt ?? DateTime.MaxValue),
+        ("status", false) => query.OrderBy(x => x.EndedAt == null),
+        ("status", true) => query.OrderByDescending(x => x.EndedAt == null),
+        _ => query.OrderByDescending(x => x.StartedAt ?? DateTime.MinValue).ThenByDescending(x => x.EndedAt ?? DateTime.MaxValue),
+    };
     private Task<IReadOnlyList<AdminQueryService.LookupItem>> SearchEmployeesAsync(string search, CancellationToken
 ct) => AdminQueries.SearchEmployeesAsync(search, ct);
     private Task<IReadOnlyList<AdminQueryService.LookupItem>> SearchPositionsAsync(string search, CancellationToken
@@ -173,6 +188,12 @@ LoadAsync();
     }
     private async Task PageChanged(int page) { _page = page; await LoadAsync(); }
     private async Task PageSizeChanged(int size) { _pageSize = size; _page = 1; await LoadAsync(); }
+    private async Task SortByAsync(string column)
+    {
+        if (_sortBy == column) _sortDesc = !_sortDesc; else { _sortBy = column; _sortDesc = false; }
+        _page = 1;
+        await LoadAsync();
+    }
     private static string FormatDate(DateTime? value) => value is null ? "—" :
 value.Value.ToLocalTime().ToString("yyyy/MM/dd HH:mm");
     private sealed record HistoryRow(Guid Id, string EmployeeName, string Username, string PositionName, DateTime?
diff --git a/src/YasPortal.Web/Components/Pages/Admin/AuditLog.razor b/src/YasPortal.Web/Components/Pages/Admin/AuditLog.razor
index ab353d3..851aa01 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/AuditLog.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/AuditLog.razor
@@ -74,7 +74,14 @@
         else{
             <div class="data-table-wrap" data-no-auto-paginate>
                 <table class="data-table data-table-bordered-rows">
-                    <thead><tr><th>زمان</th><th>کاربر</th><th>سمت فعال</th><th>عملیات</th><th>موجودیت</th><th>جزئیات</th></tr></thead>
+                    <thead><tr>
+                        <SortableHeader Text="زمان" Column="time" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                        <SortableHeader Text="کاربر" Column="actor" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                        <SortableHeader Text="سمت فعال" Column="position" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                        <SortableHeader Text="عملیات" Column="action" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                        <SortableHeader Text="موجودیت" Column="entity" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                        <th>جزئیات</th>
+                    </tr></thead>
                     <tbody>
                     @foreach(var row in _rows){
                         var local=row.OccurredAtUtc.ToLocalTime();
@@ -125,6 +132,7 @@
     private int _totalCount;
     private bool _loading;
     private bool _hasSearched;
+    private string _sortBy = "time"; private bool _sortDesc = true;
     private CancellationTokenSource? _loadCts;
     private int TotalPages => Math.Max(1, (int)Math.Ceiling(_totalCount / (double)_pageSize));
 
@@ -143,7 +151,19 @@
             if(!string.IsNullOrWhiteSpace(et))query=query.Where(x=>x.EmployeeId.HasValue&&db.Employees.Any(e=>e.Id==x.EmployeeId&&e.FullName.Contains(et)));
             if(!string.IsNullOrWhiteSpace(pt))query=query.Where(x=>x.ActivePositionId.HasValue&&db.Positions.Any(p=>p.Id==x.ActivePositionId&&p.Name.Contains(pt)));
             _totalCount=await query.CountAsync(token);if(_page>TotalPages)_page=TotalPages;
-            var entries=await query.OrderByDescending(x=>x.OccurredAtUtc).Skip((_page-1)*_pageSize).Take(_pageSize).ToListAsync(token);
+            query=(_sortBy,_sortDesc)switch{
+                ("time",false)=>query.OrderBy(x=>x.OccurredAtUtc),
+                ("actor",false)=>query.OrderBy(x=>x.EmployeeId.HasValue?db.Employees.Where(e=>e.Id==x.EmployeeId).Select(e=>e.FullName).FirstOrDefault():null),
+                ("actor",true)=>query.OrderByDescending(x=>x.EmployeeId.HasValue?db.Employees.Where(e=>e.Id==x.EmployeeId).Select(e=>e.FullName).FirstOrDefault():null),
+                ("position",false)=>query.OrderBy(x=>x.ActivePositionId.HasValue?db.Positions.Where(p=>p.Id==x.ActivePositionId).Select(p=>p.Name).FirstOrDefault():null),
+                ("position",true)=>query.OrderByDescending(x=>x.ActivePositionId.HasValue?db.Positions.Where(p=>p.Id==x.ActivePositionId).Select(p=>p.Name).FirstOrDefault():null),
+                ("action",false)=>query.OrderBy(x=>x.Action),
+                ("action",true)=>query.OrderByDescending(x=>x.Action),
+                ("entity",false)=>query.OrderBy(x=>x.EntityType),
+                ("entity",true)=>query.OrderByDescending(x=>x.EntityType),
+                _=>query.OrderByDescending(x=>x.OccurredAtUtc),
+            };
+            var entries=await query.Skip((_page-1)*_pageSize).Take(_pageSize).ToListAsync(token);
             var eids=entries.Where(x=>x.EmployeeId.HasValue).Select(x=>x.EmployeeId!.Value).Distinct().ToList();var pids=entries.Where(x=>x.ActivePositionId.HasValue).Select(x=>x.ActivePositionId!.Value).Distinct().ToList();
             var employees=await db.Employees.AsNoTracking().Where(x=>eids.Contains(x.Id)).ToDictionaryAsync(x=>x.Id,x=>x.FullName,token);var positions=await db.Positions.AsNoTracking().Where(x=>pids.Contains(x.Id)).ToDictionaryAsync(x=>x.Id,x=>x.Name,token);
             var permissionEntityIds=new List<Guid>();var employeeEntityIds=new List<Guid>();var positionEntityIds=new List<Guid>();var organizationEntityIds=new List<Guid>();var groupEntityIds=new List<Guid>();
@@ -162,6 +182,7 @@
     private async Task ClearOrCancelAsync(){if(_loading){_loadCts?.Cancel();return;}_employeeSearch="";_positionSearch="";_entityType="";_action="";_fromDate=null;_toDate=null;_page=1;_totalCount=0;_rows.Clear();_selected=null;_hasSearched=false;await InvokeAsync(StateHasChanged);}
     private async Task OnPageSizeChangedAsync(int size){_pageSize=size;_page=1;if(_hasSearched)await LoadPageAsync(1);}
     private async Task GoToPageAsync(int p){if(_hasSearched)await LoadPageAsync(p);}
+    private async Task SortByAsync(string column){if(_sortBy==column)_sortDesc=!_sortDesc;else{_sortBy=column;_sortDesc=false;}await LoadPageAsync(1);}
     private Dictionary<Guid,string> _idNameMap=new();
     private async Task ShowDetailsAsync(AuditRow r)
     {
diff --git a/src/YasPortal.Web/Components/Pages/Admin/Organizations.razor b/src/YasPortal.Web/Components/Pages/Admin/Organizations.razor
index 238b367..40f46b7 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/Organizations.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/Organizations.razor
@@ -76,9 +76,9 @@ class="bx bx-save"></i> @(_editingId is null ? "ایجاد سازمان" : "ذخ
                             <table class="data-table data-table-bordered-rows">
                                 <thead>
                                     <tr>
-                                        <th>سازمان</th>
-                                        <th>کارکنان</th>
-                                        <th>وضعیت</th>
+                                        <SortableHeader Text="سازمان" Column="name" CurrentSort="@_orgSortBy" CurrentDescending="@_orgSortDesc" OnSort="SortOrganizationsByAsync" />
+                                        <SortableHeader Text="کارکنان" Column="employees" CurrentSort="@_orgSortBy" CurrentDescending="@_orgSortDesc" OnSort="SortOrganizationsByAsync" />
+                                        <SortableHeader Text="وضعیت" Column="status" CurrentSort="@_orgSortBy" CurrentDescending="@_orgSortDesc" OnSort="SortOrganizationsByAsync" />
                                         <th>عملیات</th>
                                     </tr>
                                 </thead>
@@ -196,6 +196,7 @@ class="bx bx-save"></i> @(_editingId is null ? "ایجاد سازمان" : "ذخ
     private string _name = string.Empty, _organizationSearch = "", _employeeSearch = "";
     private int
 _organizationPage = 1, _organizationPageSize = 20, _organizationTotal, _activeOrganizationCount;
+    private string? _orgSortBy; private bool _orgSortDesc;
     private bool _organizationLoading, _employeeLoading;
     private List<AdminQueryService.EmployeeAssignmentRow> _allEmployees = [];
     private readonly HashSet<string> _collapsedOrgGroups = [];
@@ -213,18 +214,24 @@ _organizationPage = 1, _organizationPageSize = 20, _organizationTotal, _activeOr
         _organizationLoading = true; try
         {
             var r = await
-AdminQueries.GetOrganizationsAsync(_organizationPage, _organizationPageSize, _organizationSearch); _organizations =
+AdminQueries.GetOrganizationsAsync(_organizationPage, _organizationPageSize, _organizationSearch, _orgSortBy, _orgSortDesc); _organizations =
 r.Items.ToList(); _organizationTotal = r.TotalCount; if (_organizationTotal > 0 && _organizationPage > Math.Max(1,
 (int)Math.Ceiling(_organizationTotal / (double)_organizationPageSize)))
             {
                 _organizationPage = 1; var
 reset = await
-AdminQueries.GetOrganizationsAsync(_organizationPage, _organizationPageSize, _organizationSearch); _organizations =
+AdminQueries.GetOrganizationsAsync(_organizationPage, _organizationPageSize, _organizationSearch, _orgSortBy, _orgSortDesc); _organizations =
 reset.Items.ToList(); _organizationTotal = reset.TotalCount;
             }
         }
         finally { _organizationLoading = false; }
     }
+    private async Task SortOrganizationsByAsync(string column)
+    {
+        if (_orgSortBy == column) _orgSortDesc = !_orgSortDesc; else { _orgSortBy = column; _orgSortDesc = false; }
+        _organizationPage = 1;
+        await LoadOrganizationsAsync();
+    }
     private async Task LoadEmployeesAsync()
     {
         _employeeLoading = true; try
diff --git a/src/YasPortal.Web/Components/Pages/Admin/Users.razor b/src/YasPortal.Web/Components/Pages/Admin/Users.razor
index d62642f..52eb297 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/Users.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/Users.razor
@@ -88,11 +88,11 @@
                                 <table class="data-table data-table-bordered-rows">
                                     <thead>
                                         <tr>
-                                            <th>کاربر</th>
-                                            <th>نوع</th>
-                                            <th>سازمان</th>
+                                            <SortableHeader Text="کاربر" Column="name" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="نوع" Column="type" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
+                                            <SortableHeader Text="سازمان" Column="organization" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
                                             <th>سمت‌ها</th>
-                                            <th>وضعیت</th>
+                                            <SortableHeader Text="وضعیت" Column="status" CurrentSort="@_sortBy" CurrentDescending="@_sortDesc" OnSort="SortByAsync" />
                                             <th>عملیات</th>
                                         </tr>
                                     </thead>
@@ -175,6 +175,7 @@
     private string _username = "", _fullName = "", _password = "", _organizationName = "", _search = "";
     private bool _isAdmin, _loading;
     private int _page = 1, _pageSize = 20, _totalCount, _activeCount, _adminCount;
+    private string? _sortBy; private bool _sortDesc;
 
     protected override async Task OnInitializedAsync() => await LoadAsync();
     private async Task LoadAsync()
@@ -182,7 +183,7 @@
         _loading = true; try
         {
             var result = await
-AdminQueries.GetEmployeesAsync(_page, _pageSize, _search); _employees = result.Items.ToList(); _totalCount =
+AdminQueries.GetEmployeesAsync(_page, _pageSize, _search, _sortBy, _sortDesc); _employees = result.Items.ToList(); _totalCount =
 result.TotalCount; await
 using var db = await DbFactory.CreateDbContextAsync(); _activeCount = await
 db.Employees.CountAsync(x => x.IsActive); _adminCount = await
@@ -201,6 +202,12 @@ LoadAsync();
     private async Task ClearSearch() { _search = ""; _page = 1; await LoadAsync(); }
     private async Task PageChanged(int page) { _page = page; await LoadAsync(); }
     private async Task PageSizeChanged(int size) { _pageSize = size; _page = 1; await LoadAsync(); }
+    private async Task SortByAsync(string column)
+    {
+        if (_sortBy == column) _sortDesc = !_sortDesc; else { _sortBy = column; _sortDesc = false; }
+        _page = 1;
+        await LoadAsync();
+    }
     private Task<IReadOnlyList<AdminQueryService.LookupItem>> SearchOrganizationsAsync(string search, CancellationToken
 ct) => AdminQueries.SearchOrganizationsAsync(search, true, ct);
     private Task OrganizationChanged(Guid? id) { _organizationId = id; return Task.CompletedTask; }
diff --git a/src/YasPortal.Web/Components/Shared/SortableHeader.razor b/src/YasPortal.Web/Components/Shared/SortableHeader.razor
new file mode 100644
index 0000000..ce019dc
--- /dev/null
+++ b/src/YasPortal.Web/Components/Shared/SortableHeader.razor
@@ -0,0 +1,24 @@
+<th class="sortable-th" @onclick="HandleClick">
+    <span class="sortable-th-inner">
+        @Text
+        <i class="bx @IconClass"></i>
+    </span>
+</th>
+
+@code {
+    /// <summary>Column label shown in the header.</summary>
+    [Parameter] public string Text { get; set; } = "";
+    /// <summary>Sort key this header represents (matches what the page passes back on OnSort).</summary>
+    [Parameter, EditorRequired] public string Column { get; set; } = "";
+    /// <summary>The page's currently active sort column.</summary>
+    [Parameter] public string? CurrentSort { get; set; }
+    /// <summary>Whether the currently active sort column is descending.</summary>
+    [Parameter] public bool CurrentDescending { get; set; }
+    /// <summary>Raised with this header's Column when clicked; the page decides direction toggling.</summary>
+    [Parameter] public EventCallback<string> OnSort { get; set; }
+
+    private bool IsActive => CurrentSort == Column;
+    private string IconClass => IsActive ? (CurrentDescending ? "bx-sort-down" : "bx-sort-up") : "bx-sort-alt-2";
+
+    private Task HandleClick() => OnSort.InvokeAsync(Column);
+}
diff --git a/src/YasPortal.Web/Services/AdminQueryService.cs b/src/YasPortal.Web/Services/AdminQueryService.cs
index af25321..301c787 100644
--- a/src/YasPortal.Web/Services/AdminQueryService.cs
+++ b/src/YasPortal.Web/Services/AdminQueryService.cs
@@ -131,7 +131,7 @@ public sealed class AdminQueryService(IDbContextFactory<ApplicationDbContext> db
             .ToListAsync(ct);
     }
 
-    public async Task<PageResult<EmployeePageRow>> GetEmployeesAsync(int page, int pageSize, string? search, CancellationToken ct = default)
+    public async Task<PageResult<EmployeePageRow>> GetEmployeesAsync(int page, int pageSize, string? search, string? sortBy = null, bool sortDesc = false, CancellationToken ct = default)
     {
         page = Page(page);
         pageSize = Size(pageSize);
@@ -139,14 +139,25 @@ public sealed class AdminQueryService(IDbContextFactory<ApplicationDbContext> db
         await using var db = await dbFactory.CreateDbContextAsync(ct);
         var query = db.Employees.AsNoTracking().Where(x => q == "" || x.FullName.Contains(q) || x.Username.Contains(q));
         var total = await query.CountAsync(ct);
-        var rows = await query.OrderBy(x => x.FullName).Skip((page - 1) * pageSize).Take(pageSize).Select(x => new EmployeePageRow(x.Id, x.Username, x.FullName, db.Organizations.Where(o => o.Id == x.OrganizationId).Select(o => (string?)o.Name).FirstOrDefault(), x.IsActive, x.IsAdmin)).ToListAsync(ct);
+        query = (sortBy, sortDesc) switch
+        {
+            ("type", false) => query.OrderBy(x => x.IsAdmin).ThenBy(x => x.FullName),
+            ("type", true) => query.OrderByDescending(x => x.IsAdmin).ThenBy(x => x.FullName),
+            ("status", false) => query.OrderBy(x => x.IsActive).ThenBy(x => x.FullName),
+            ("status", true) => query.OrderByDescending(x => x.IsActive).ThenBy(x => x.FullName),
+            ("organization", false) => query.OrderBy(x => db.Organizations.Where(o => o.Id == x.OrganizationId).Select(o => o.Name).FirstOrDefault()).ThenBy(x => x.FullName),
+            ("organization", true) => query.OrderByDescending(x => db.Organizations.Where(o => o.Id == x.OrganizationId).Select(o => o.Name).FirstOrDefault()).ThenBy(x => x.FullName),
+            ("name", true) => query.OrderByDescending(x => x.FullName),
+            _ => query.OrderBy(x => x.FullName),
+        };
+        var rows = await query.Skip((page - 1) * pageSize).Take(pageSize).Select(x => new EmployeePageRow(x.Id, x.Username, x.FullName, db.Organizations.Where(o => o.Id == x.OrganizationId).Select(o => (string?)o.Name).FirstOrDefault(), x.IsActive, x.IsAdmin)).ToListAsync(ct);
         var employeeIds = rows.Select(x => x.Id).ToArray();
         var assignments = await db.EmployeePositions.AsNoTracking().Where(x => employeeIds.Contains(x.EmployeeId) && x.EndedAt == null).OrderBy(x => x.Position.Name).Select(x => new { x.EmployeeId, x.PositionId, x.Position.Name }).ToListAsync(ct);
         var positionsByEmployee = assignments.GroupBy(x => x.EmployeeId).ToDictionary(g => g.Key, g => g.Select(x => new ActivePositionRow(x.PositionId, x.Name)).ToList() as IReadOnlyList<ActivePositionRow>);
         var items = rows.Select(x => x with { ActivePositions = positionsByEmployee.TryGetValue(x.Id, out var positions) ? positions : Array.Empty<ActivePositionRow>() }).ToList();
         return new(items, total, page, pageSize);
     }
-    public async Task<PageResult<OrganizationPageRow>> GetOrganizationsAsync(int page, int pageSize, string? search, CancellationToken ct = default)
+    public async Task<PageResult<OrganizationPageRow>> GetOrganizationsAsync(int page, int pageSize, string? search, string? sortBy = null, bool sortDesc = false, CancellationToken ct = default)
     {
         page = Page(page);
         pageSize = Size(pageSize);
@@ -154,7 +165,16 @@ public sealed class AdminQueryService(IDbContextFactory<ApplicationDbContext> db
         await using var db = await dbFactory.CreateDbContextAsync(ct);
         var query = db.Organizations.AsNoTracking().Where(x => q == "" || x.Name.Contains(q));
         var total = await query.CountAsync(ct);
-        var items = await query.OrderBy(x => x.Name).Skip((page - 1) * pageSize).Take(pageSize).Select(x => new OrganizationPageRow(x.Id, x.Name, x.IsActive, x.Employees.Count)).ToListAsync(ct);
+        var ordered = (sortBy, sortDesc) switch
+        {
+            ("status", false) => query.OrderBy(x => x.IsActive).ThenBy(x => x.Name),
+            ("status", true) => query.OrderByDescending(x => x.IsActive).ThenBy(x => x.Name),
+            ("employees", false) => query.OrderBy(x => x.Employees.Count).ThenBy(x => x.Name),
+            ("employees", true) => query.OrderByDescending(x => x.Employees.Count).ThenBy(x => x.Name),
+            ("name", true) => query.OrderByDescending(x => x.Name),
+            _ => query.OrderBy(x => x.Name),
+        };
+        var items = await ordered.Skip((page - 1) * pageSize).Take(pageSize).Select(x => new OrganizationPageRow(x.Id, x.Name, x.IsActive, x.Employees.Count)).ToListAsync(ct);
         return new(items, total, page, pageSize);
     }
     public async Task<PageResult<PermissionPageRow>> GetPermissionsAsync(int page, int pageSize, string? search, string? usage, CancellationToken ct = default)
diff --git a/src/YasPortal.Web/wwwroot/css/admin/admin-tables-and-lists.css b/src/YasPortal.Web/wwwroot/css/admin/admin-tables-and-lists.css
index a758ced..5007ef1 100644
--- a/src/YasPortal.Web/wwwroot/css/admin/admin-tables-and-lists.css
+++ b/src/YasPortal.Web/wwwroot/css/admin/admin-tables-and-lists.css
@@ -192,3 +192,11 @@
     .admin-page .selection-toolbar > div .btn { flex:1 1 0; }
     .admin-page .effective-row { align-items:flex-start; flex-direction:column; }
 }
+
+/* Sortable table headers */
+.sortable-th{cursor:pointer;user-select:none;-webkit-user-select:none}
+.sortable-th:hover{background:#eef1f6}
+.sortable-th-inner{display:inline-flex;align-items:center;gap:4px}
+.sortable-th-inner i{font-size:13px;color:#98a2b3}
+.sortable-th:hover .sortable-th-inner i{color:#667085}
+
