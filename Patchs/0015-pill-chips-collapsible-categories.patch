diff --git a/src/YasPortal.Web/Components/App.razor b/src/YasPortal.Web/Components/App.razor
index 4aeb2c3..108b2a1 100644
--- a/src/YasPortal.Web/Components/App.razor
+++ b/src/YasPortal.Web/Components/App.razor
@@ -10,7 +10,7 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <base href="/" />
     <link rel="icon" type="image/svg+xml" href="favicon.svg" />
-    <link rel="stylesheet" href="css/app.css?v=20260826-7" />
+    <link rel="stylesheet" href="css/app.css?v=20260826-8" />
     <link rel="stylesheet" href="lib/boxicons/css/boxicons.min.css" />
     <HeadOutlet @rendermode="InteractiveServer" />
 </head>
diff --git a/src/YasPortal.Web/Components/Pages/Admin/AccessManagement.razor b/src/YasPortal.Web/Components/Pages/Admin/AccessManagement.razor
index babd301..b01d8a6 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/AccessManagement.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/AccessManagement.razor
@@ -100,10 +100,11 @@
 
                                 <div class="permission-sections">@foreach(var area in
                                     _permissions.GroupBy(x => PermissionArea(x.Code)).OrderBy(x => x.Key)){
-                                        <section class="permission-section">
-                                            <div class="permission-section-heading">
+                                        var collapsed = _collapsedAreas.Contains(area.Key);
+                                        <section class="permission-section @(collapsed ? "collapsed" : "")">
+                                            <div class="permission-section-heading" @onclick="() => ToggleArea(area.Key)">
                                                 <div><i class="bx bx-folder-open"></i><strong>@area.Key</strong></div><span
-                                                class="badge badge-muted">@area.Count() مجوز</span>
+                                                class="badge badge-muted">@area.Count() مجوز</span><i class="bx bx-chevron-down chevron"></i>
                                             </div>
                                             <div class="permission-grid">@foreach(var p in area){var
                                                 selected = _directPermissionIds.Contains(p.Id);
@@ -211,6 +212,8 @@ _effectiveViaGroupIds = [];
     private int _permissionTotal, _groupTotal; private string _permissionSearch = "", _groupSearch = "", _effectiveSearch =
 "";
     private string? _statusMessage; private bool _permissionLoading, _groupLoading, _saving;
+    private HashSet<string> _collapsedAreas = [];
+    private void ToggleArea(string key) { if (!_collapsedAreas.Add(key)) _collapsedAreas.Remove(key); }
     private Task<IReadOnlyList<AdminQueryService.LookupItem>> SearchEmployeesAsync(string search, CancellationToken
 ct) => AdminQueries.SearchEmployeesAsync(search, ct);
     private Task<IReadOnlyList<AdminQueryService.LookupItem>> SearchEmployeePositionsAsync(string search, CancellationToken
diff --git a/src/YasPortal.Web/Components/Pages/Admin/PermissionGroups.razor b/src/YasPortal.Web/Components/Pages/Admin/PermissionGroups.razor
index 0ae35d1..158250a 100644
--- a/src/YasPortal.Web/Components/Pages/Admin/PermissionGroups.razor
+++ b/src/YasPortal.Web/Components/Pages/Admin/PermissionGroups.razor
@@ -121,9 +121,10 @@
 
                 <div class="permission-sections">@foreach(var area in
                     _permissions.GroupBy(x => PermissionCategory(x.Code)).OrderBy(x => x.Key)){
-                        <section class="permission-section">
-                            <div class="permission-section-heading">
-                                <div><i class="bx bx-folder-open"></i><strong>@area.Key</strong></div><span class="badge badge-muted">@area.Count() مجوز</span>
+                        var collapsed = _collapsedAreas.Contains(area.Key);
+                        <section class="permission-section @(collapsed ? "collapsed" : "")">
+                            <div class="permission-section-heading" @onclick="() => ToggleArea(area.Key)">
+                                <div><i class="bx bx-folder-open"></i><strong>@area.Key</strong></div><span class="badge badge-muted">@area.Count() مجوز</span><i class="bx bx-chevron-down chevron"></i>
                             </div>
                             <div class="permission-grid">@foreach(var permission in area){var
                                 granted = _selectedPermissionIds.Contains(permission.Id);
@@ -161,6 +162,8 @@ else
     _groupLoading, _permissionLoading, _saving, _onlySelected; private HashSet<Guid> _selectedPermissionIds = []; private
 string
     SelectedGroupName => _groups.FirstOrDefault(x => x.Id == _selectedGroupId)?.Name ?? "گروه";
+    private HashSet<string> _collapsedAreas = [];
+    private void ToggleArea(string key) { if (!_collapsedAreas.Add(key)) _collapsedAreas.Remove(key); }
     protected override async Task OnInitializedAsync() => await LoadGroupsAsync();
     private async Task LoadGroupsAsync()
     {
diff --git a/src/YasPortal.Web/wwwroot/css/app.css b/src/YasPortal.Web/wwwroot/css/app.css
index f048982..0d1301c 100644
--- a/src/YasPortal.Web/wwwroot/css/app.css
+++ b/src/YasPortal.Web/wwwroot/css/app.css
@@ -342,12 +342,16 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 .admin-page .selection-toolbar > div { display:flex; align-items:center; gap:5px; flex-wrap:nowrap; }
 .admin-page .permission-sections { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:8px; align-items:start; }
 .admin-page .permission-section { overflow:hidden; border:1px solid #e5e7eb; border-radius:9px; background:#fff; }
-.admin-page .permission-section-heading { display:flex; align-items:center; justify-content:space-between; gap:8px; padding:9px 12px; border-bottom:1px solid var(--admin-border, #e5e7eb); background:#f8fafc; }
+.admin-page .permission-section-heading { display:flex; align-items:center; justify-content:space-between; gap:8px; padding:9px 12px; border-bottom:1px solid var(--admin-border, #e5e7eb); background:#f8fafc; cursor:pointer; user-select:none; }
+.admin-page .permission-section-heading:hover { background:#f2f5fa; }
+.admin-page .permission-section-heading .chevron { transition:transform .15s ease; color:var(--admin-muted, #667085); font-size:16px; }
+.admin-page .permission-section.collapsed .permission-section-heading .chevron { transform:rotate(-90deg); }
+.admin-page .permission-section.collapsed .permission-grid { display:none; }
 .admin-page .permission-section-heading > div { display:flex; align-items:center; gap:8px; }
 .admin-page .permission-section-heading > div i { width:26px; height:26px; flex:0 0 26px; border-radius:8px; background:var(--admin-primary-soft, #eef3ff); color:var(--admin-primary, #3157d5); display:grid; place-items:center; font-size:14px; }
 .admin-page .permission-section-heading > div strong { font-size:12.5px; color:var(--admin-text, #172033); }
 .admin-page .permission-section-heading span { color:#667085; font-size:10px; }
-.admin-page .permission-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:7px; padding:7px; }
+.admin-page .permission-grid { display:flex; flex-wrap:wrap; gap:8px; padding:7px; }
 .admin-page .permission-tile,
 .admin-page .group-tile { position:relative; display:flex; align-items:center; gap:7px; min-width:0; padding:7px 8px; border:1px solid #e5e7eb; border-radius:8px; background:#fff; cursor:pointer; }
 .admin-page .permission-tile:hover,
@@ -476,7 +480,6 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 }
 @media (max-width:560px) {
     .admin-page .filter-bar,
-    .admin-page .permission-grid,
     .admin-page .group-list { grid-template-columns:1fr; }
     .admin-page .selection-toolbar,
     .admin-page .panel-heading { align-items:stretch; flex-direction:column; }
@@ -490,14 +493,14 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 /* Shared permission checkbox — identical on Admin Access and Permission Groups. */
 .admin-page .permission-choice,
 .admin-page .group-choice {
-    display: grid;
-    grid-template-columns: 18px minmax(0, 1fr);
+    display: inline-flex;
     align-items: center;
-    gap: 8px;
-    min-width: 0;
-    padding: 10px 12px;
-    border: 1px solid var(--admin-border, #e5e7eb);
-    border-radius: 10px;
+    gap: 0;
+    width: auto;
+    max-width: 100%;
+    padding: 8px 16px;
+    border: 1.5px solid var(--admin-border-strong, #d8e0e8);
+    border-radius: 999px;
     background: var(--admin-surface, #fff);
     cursor: pointer;
     transition: border-color .12s ease, background .12s ease;
@@ -505,14 +508,13 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 
 .admin-page .permission-choice:hover,
 .admin-page .group-choice:hover {
-    border-color: var(--admin-border-strong, #d8e0e8);
-    background: #fafcff;
+    border-color: var(--admin-primary, #3157d5);
 }
 
 .admin-page .permission-choice.selected,
 .admin-page .group-choice.selected {
-    border-color: transparent;
-    background: var(--admin-primary-soft, #eef3ff);
+    border-color: var(--admin-primary, #3157d5);
+    background: var(--admin-primary, #3157d5);
 }
 
 .admin-page .permission-choice input,
@@ -523,29 +525,12 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 }
 
 .admin-page .choice-box {
-    width: 18px;
-    height: 18px;
-    display: grid;
-    place-items: center;
-    border: 1.5px solid var(--admin-border-strong, #d0d5dd);
-    border-radius: 5px;
-    color: transparent;
-    background: transparent;
-    flex: 0 0 18px;
-}
-
-.admin-page .selected .choice-box {
-    border-color: var(--admin-primary, #3157d5);
-    background: var(--admin-primary, #3157d5);
-    color: #fff;
-}
-
-.admin-page .choice-box i {
-    font-size: 12px;
+    display: none;
 }
 
 .admin-page .choice-text {
     min-width: 0;
+    max-width: 100%;
     display: flex;
     flex-direction: column;
     gap: 1px;
@@ -562,7 +547,7 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 
 .admin-page .selected .choice-text strong {
     font-weight: 600;
-    color: var(--admin-primary, #3157d5);
+    color: #fff;
 }
 
 
