diff --git a/src/YasPortal.Web/Components/App.razor b/src/YasPortal.Web/Components/App.razor
index aaec9bd..821b95a 100644
--- a/src/YasPortal.Web/Components/App.razor
+++ b/src/YasPortal.Web/Components/App.razor
@@ -10,7 +10,7 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <base href="/" />
     <link rel="icon" type="image/svg+xml" href="favicon.svg" />
-    <link rel="stylesheet" href="css/app.css?v=20260827-6" />
+    <link rel="stylesheet" href="css/app.css?v=20260828-1" />
     <link rel="stylesheet" href="lib/boxicons/css/boxicons.min.css" />
     <HeadOutlet @rendermode="InteractiveServer" />
 </head>
@@ -18,7 +18,6 @@
 <body>
     <ApplicationShell @rendermode="InteractiveServer" />
     <script src="_framework/blazor.web.js"></script>
-    <script src="js/searchable-select.js"></script>
     <script src="js/admin-ui.js"></script>
     <script src="js/active-position-dropdown.js"></script>
     <script src="js/server-lookup.js"></script>
diff --git a/src/YasPortal.Web/wwwroot/css/app.css b/src/YasPortal.Web/wwwroot/css/app.css
index 727692d..e8004d0 100644
--- a/src/YasPortal.Web/wwwroot/css/app.css
+++ b/src/YasPortal.Web/wwwroot/css/app.css
@@ -20,7 +20,29 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 @media(max-width:520px){.topbar{height:58px;padding-inline:10px}.brand{font-size:16px}.brand-mark{width:34px;height:34px}.topbar-user small{display:none}.user-avatar{width:34px;height:34px}.application-body{min-height:0}.sidebar{padding:7px 8px}.sidebar-nav a{font-size:12px;padding:9px 10px}.content{padding:15px 10px}.page-header{margin-bottom:15px}.page-title{font-size:20px}.page-subtitle{font-size:12px}.login-card{padding:22px 18px;border-radius:15px}}
 
 /* Modal layering: dialogs must always sit above shared date pickers. */
-.admin-modal-overlay{z-index:3000!important}.admin-modal{z-index:3001!important}
+.admin-modal-overlay{position:fixed;inset:0;z-index:3000!important;background:rgba(16,24,40,.55);display:flex;align-items:center;justify-content:center;padding:24px}
+.admin-modal{z-index:3001!important;background:var(--admin-surface,#fff);border-radius:var(--admin-radius,14px);box-shadow:var(--admin-shadow,0 8px 28px rgba(16,24,40,.055)),0 24px 60px rgba(16,24,40,.25);width:100%;max-width:720px;max-height:min(85vh,820px);display:flex;flex-direction:column;overflow:hidden}
+.admin-modal-header{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:16px 18px;border-bottom:1px solid var(--admin-border,#e5eaf0);flex:0 0 auto}
+.admin-modal-header h2{margin:2px 0;font-size:14px;color:var(--admin-text,#172033)}
+.admin-modal-header .muted{font-size:11px}
+.admin-modal__body{padding:16px 18px;overflow-y:auto;flex:1 1 auto;display:flex;flex-direction:column;gap:14px}
+.admin-modal-footer{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 18px;border-top:1px solid var(--admin-border,#e5eaf0);flex:0 0 auto;background:#fafbfc}
+.admin-modal-footer span{font-size:10.5px;color:var(--admin-muted,#667085)}
+.admin-meta-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}
+.admin-meta-card{border:1px solid var(--admin-border,#e5eaf0);border-radius:10px;padding:9px 11px;display:flex;flex-direction:column;gap:3px;min-width:0}
+.admin-meta-card span{font-size:10px;color:var(--admin-muted,#667085)}
+.admin-meta-card strong{font-size:12px;color:var(--admin-text,#172033);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
+.admin-meta-card .entity-id{font-size:10px;color:var(--admin-muted,#667085)}
+.admin-changes{display:grid;grid-template-columns:1fr auto 1fr;gap:8px;align-items:start}
+.admin-change-panel{border:1px solid var(--admin-border,#e5eaf0);border-radius:10px;overflow:hidden;min-width:0}
+.admin-change-header{display:flex;align-items:center;gap:6px;padding:8px 11px;font-size:11px;font-weight:700;color:var(--admin-text,#172033);background:#f8fafc;border-bottom:1px solid var(--admin-border,#e5eaf0)}
+.admin-change-arrow{display:flex;align-items:center;justify-content:center;padding-top:14px;color:var(--admin-muted,#667085);font-size:16px}
+.admin-change-empty{min-height:120px;padding:20px}
+.admin-json-key{display:block;font-size:11px;font-weight:600;color:var(--admin-text,#172033)}
+.admin-json-value{display:block;font-size:11px;color:var(--admin-muted,#667085);word-break:break-word;margin-top:2px}
+.admin-json-fallback{padding:11px;font-size:11px}
+.admin-json-fallback pre{margin:6px 0 0;white-space:pre-wrap;word-break:break-word;font-size:10.5px;color:var(--admin-muted,#667085)}
+@media(max-width:700px){.admin-meta-grid{grid-template-columns:1fr}.admin-changes{grid-template-columns:1fr}.admin-change-arrow{display:none}}
 
 
 /* ----- components.css ----- */
@@ -186,91 +208,6 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 }
 
 
-/* ----- searchable-select.css ----- */
-.searchable-select {
-    position: relative;
-    width: 100%;
-}
-
-.searchable-select .searchable-native-select {
-    position: absolute !important;
-    width: 1px !important;
-    height: 1px !important;
-    padding: 0 !important;
-    margin: -1px !important;
-    overflow: hidden !important;
-    clip: rect(0, 0, 0, 0) !important;
-    white-space: nowrap !important;
-    border: 0 !important;
-}
-
-.searchable-select-input {
-    width: 100%;
-    min-height: 42px;
-    padding-left: 40px !important;
-    cursor: text;
-    background-image: linear-gradient(45deg, transparent 50%, currentColor 50%), linear-gradient(135deg, currentColor 50%, transparent 50%);
-    background-position: calc(100% - 18px) 18px, calc(100% - 13px) 18px;
-    background-size: 5px 5px, 5px 5px;
-    background-repeat: no-repeat;
-}
-
-.searchable-select-input:disabled {
-    cursor: not-allowed;
-    opacity: .65;
-}
-
-.searchable-select-dropdown {
-    position: absolute;
-    z-index: 3000;
-    top: calc(100% + 5px);
-    right: 0;
-    left: 0;
-    max-height: 320px;
-    overflow-y: auto;
-    padding: 6px;
-    border: 1px solid var(--border, #e5e7eb);
-    border-radius: 12px;
-    background: var(--card-bg, #fff);
-    box-shadow: 0 14px 35px rgba(15, 23, 42, .14);
-}
-
-.searchable-select-option {
-    display: block;
-    width: 100%;
-    border: 0;
-    border-radius: 9px;
-    padding: 10px 12px;
-    text-align: right;
-    font: inherit;
-    background: transparent;
-    color: inherit;
-    cursor: pointer;
-}
-
-.searchable-select-option:hover,
-.searchable-select-option.active {
-    background: rgba(37, 99, 235, .09);
-}
-
-.searchable-select-option.selected {
-    font-weight: 700;
-}
-
-.searchable-select-empty,
-.searchable-select-more {
-    padding: 12px;
-    color: var(--text-muted, #6b7280);
-    text-align: center;
-    font-size: 13px;
-}
-
-.searchable-select-more {
-    border-top: 1px solid var(--border, #e5e7eb);
-    margin-top: 4px;
-}
-
-
 /* ----- admin-ui.css ----- */
 /* YasPortal Admin UI — shared RTL design system */
 :root{--admin-bg:#f6f8fb;--admin-surface:#fff;--admin-border:#e5eaf0;--admin-border-strong:#d8e0e8;--admin-text:#172033;--admin-muted:#667085;--admin-primary:#3157d5;--admin-primary-dark:#274bc4;--admin-primary-soft:#eef3ff;--admin-success:#16845b;--admin-success-soft:#eaf8f1;--admin-danger:#c53b4d;--admin-danger-soft:#fff0f2;--admin-radius:14px;--admin-shadow:0 8px 28px rgba(16,24,40,.055)}
@@ -280,7 +217,7 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 .stat-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-bottom:16px}.stat-card{display:grid;grid-template-columns:auto minmax(0,1fr) auto;grid-template-rows:1fr;align-items:center;gap:11px}.stat-card .stat-content{min-width:0;display:flex;flex-direction:column;align-items:flex-start;justify-content:center;gap:1px;flex:0 0 auto}.stat-card .stat-label{grid-column:2;grid-row:1}.stat-card .stat-value{grid-column:3;grid-row:1;justify-self:start}.stat-card .stat-value-left{text-align:left}.stat-card .stat-value-center{text-align:center}.stat-card .stat-value-right{text-align:right}.stat-card .stat-icon{grid-column:1;grid-row:1}
 .admin-toolbar,.card-toolbar{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:14px 16px;border-bottom:1px solid #eef1f5;background:#fff}.card-toolbar > div:first-child{min-width:0}.toolbar-tools{display:flex;align-items:center;gap:10px;flex-wrap:nowrap;flex-shrink:0}.compact-select{width:auto;flex:0 0 auto;min-width:130px}.section-title .badge{margin-right:8px;vertical-align:middle}.admin-filters{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:11px;padding:15px 16px}.admin-active-filters{display:flex;align-items:center;flex-wrap:wrap;gap:6px}.admin-table-wrap,.data-table-wrap{width:100%;overflow-x:auto;-webkit-overflow-scrolling:touch}.admin-table,.data-table,.admin-page table{width:100%;border-collapse:separate;border-spacing:0;table-layout:auto}.admin-table th,.admin-table td,.data-table th,.data-table td,.admin-page table th,.admin-page table td{padding:11px 12px;text-align:right;border-bottom:1px solid #eef1f5;vertical-align:middle}.admin-table th,.data-table th,.admin-page table th{background:#f8fafc;color:#667085;font-size:11px;font-weight:800;white-space:nowrap}.admin-table td,.data-table td,.admin-page table td{color:#344054;font-size:12.5px;line-height:1.55}.admin-table tbody tr:nth-child(odd) td,.data-table tbody tr:nth-child(odd) td,.admin-page table tbody tr:nth-child(odd) td{background:#fff}.admin-table tbody tr:nth-child(even) td,.data-table tbody tr:nth-child(even) td,.admin-page table tbody tr:nth-child(even) td{background:#f8fafc}.admin-table tbody tr:hover td,.data-table tbody tr:hover td,.admin-page table tbody tr:hover td{background:#eef4ff}.admin-table tbody tr:last-child td,.data-table tbody tr:last-child td,.admin-page table tbody tr:last-child td{border-bottom:0}
 .admin-table .entity-id,.admin-page table .entity-id{direction:ltr;text-align:left;font-family:ui-monospace,SFMono-Regular,Consolas,monospace;font-size:11px;overflow-wrap:anywhere}.admin-table .date-cell,.admin-page table .date-cell{white-space:nowrap;width:145px;min-width:145px}.admin-table .action-cell,.data-table .action-cell,.admin-page table .action-cell{display:table-cell!important;width:1%;min-width:150px;white-space:nowrap}.admin-table .action-cell .btn,.admin-table .action-cell .admin-btn,.data-table .action-cell .btn,.data-table .action-cell .admin-btn,.admin-page table .action-cell .btn,.admin-page table .action-cell .admin-btn{display:inline-flex!important;vertical-align:middle;flex:0 0 auto;margin-left:5px}.admin-pagination{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 14px;border-top:1px solid #eef1f5;background:#fafbfc}.badge{display:inline-flex;align-items:center;justify-content:center;gap:4px;min-height:25px;padding:4px 8px;border-radius:999px;font-size:11px;font-weight:700;white-space:nowrap}.badge-success{background:var(--admin-success-soft);color:var(--admin-success)}.badge-muted{background:#f2f4f7;color:#667085}.badge-danger{background:var(--admin-danger-soft);color:var(--admin-danger)}
-.admin-two-col{display:grid;grid-template-columns:340px minmax(0,1fr);gap:14px;align-items:start}.employee-list{border:1px solid #eef1f5;border-radius:12px;overflow:hidden}.employee-row{display:grid;grid-template-columns:minmax(180px,1fr) 280px 90px;align-items:center;gap:14px;padding:11px 14px;border-bottom:1px solid #eef1f5}.employee-row:last-child{border-bottom:0}.employee-info{display:flex;align-items:center;gap:10px;min-width:0}.employee-avatar{width:36px;height:36px;border-radius:10px;background:var(--admin-primary-soft);color:var(--admin-primary);display:grid;place-items:center;font-weight:800;flex:0 0 36px}.position-badge-list,.admin-filter-chips{display:flex;flex-wrap:wrap;gap:5px}.only-selected{display:inline-flex;align-items:center;gap:6px;font-size:11px;font-weight:600;color:var(--admin-muted);white-space:nowrap;cursor:pointer;user-select:none}.only-selected input{accent-color:var(--admin-primary);cursor:pointer}.position-badge{text-decoration:none}.admin-notice-card{margin-top:16px;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:14px;padding:16px}.empty-state,.admin-empty{min-height:190px;padding:40px 20px;text-align:center;color:#94a3b8}
+.admin-two-col{display:grid;grid-template-columns:340px minmax(0,1fr);gap:14px;align-items:start}.position-badge-list,.admin-filter-chips{display:flex;flex-wrap:wrap;gap:5px}.only-selected{display:inline-flex;align-items:center;gap:6px;font-size:11px;font-weight:600;color:var(--admin-muted);white-space:nowrap;cursor:pointer;user-select:none}.only-selected input{accent-color:var(--admin-primary);cursor:pointer}.position-badge{text-decoration:none}.admin-notice-card{margin-top:16px;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:14px;padding:16px}.empty-state,.admin-empty{min-height:190px;padding:40px 20px;text-align:center;color:#94a3b8}
 .access-selector,.access-panel{padding:18px;margin-bottom:14px}.selector-grid,.access-columns{display:grid;grid-template-columns:1fr 1fr;gap:14px}.effective-list{border:1px solid #e5e7eb;border-radius:11px;overflow:hidden}.effective-row{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 12px;border-bottom:1px solid #eef2f7}.effective-row:last-child{border-bottom:0}
 .subject-card{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:12px 16px;margin-bottom:10px;background:linear-gradient(135deg,#fff 0%,#f8faff 100%);border-color:#dfe5ee;box-shadow:0 5px 18px rgba(16,24,40,.045)}.subject-main{display:flex;align-items:center;gap:11px;min-width:0}.subject-icon{width:40px;height:40px;flex:0 0 40px;display:grid;place-items:center;border-radius:11px;background:var(--admin-primary-soft);color:var(--admin-primary);font-size:19px;border:1px solid #dbe4ff}.subject-main>div:last-child{display:flex;flex-direction:column;gap:1px;min-width:0}.subject-main strong{color:#101828;font-size:13px;font-weight:800;line-height:1.5}.subject-main span{color:var(--admin-muted);font-size:11px;line-height:1.5}.subject-stats{display:flex;align-items:stretch;gap:0;border:1px solid #e4e8ef;border-radius:10px;overflow:hidden;background:#fff;flex:0 0 auto}.subject-stats>div{min-width:68px;padding:6px 12px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:0;border-left:1px solid #eef1f5}.subject-stats>div:last-child{border-left:0}.subject-stats strong{font-size:15px;line-height:1.35;font-weight:800;color:#172033;font-variant-numeric:tabular-nums}.subject-stats span{font-size:10px;line-height:1.4;color:var(--admin-muted)}
 .access-panel .panel-heading{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 16px;border-bottom:1px solid #eef1f5;background:#fff}.access-panel .panel-heading>div:first-child{min-width:0}.access-panel .panel-heading h2{margin:0;color:var(--admin-text);font-size:15px;font-weight:800}.access-panel .panel-heading p{margin:3px 0 0;color:var(--admin-muted);font-size:11px}.access-panel .panel-heading-tools{display:flex;align-items:center;justify-content:flex-start;gap:8px;min-width:0;flex:0 1 auto}.access-panel .panel-heading-tools .admin-filter-chips{display:flex;align-items:center;flex-wrap:nowrap;gap:5px;flex:0 0 auto}.access-panel .panel-heading-tools .search-box{width:230px;flex:0 1 230px;min-width:170px}.access-panel .panel-heading-tools .search-box .form-control{min-height:36px}.selection-toolbar{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:10px 16px;border-bottom:1px solid #eef1f5;background:#fafbfc}.selection-actions{display:flex;gap:6px;flex-wrap:wrap}
@@ -323,17 +260,12 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
 .admin-table-bordered-rows .action-cell .admin-btn,
 .data-table-bordered-rows .action-cell .btn,
 .data-table-bordered-rows .action-cell .admin-btn { flex:0 0 auto; white-space:nowrap; }
-.admin-bordered-list { display:flex; flex-direction:column; gap:5px; }
-.admin-bordered-list > * { border:1px solid #e5eaf0; border-radius:8px; background:#fff; }
-.admin-bordered-list > *:nth-child(even) { background:#f6f8fb; }
-.admin-bordered-list > *:hover { border-color:#d8e0e8; background:#eef4ff; }
 
 /* Shared Access Management layout. */
 .admin-page .panel-heading { display:flex; align-items:flex-start; justify-content:space-between; gap:12px; margin-bottom:10px; }
 .admin-page .panel-heading > div:first-child { min-width:0; }
 .admin-page .panel-heading h2 { margin:2px 0 4px; }
 .admin-page .panel-heading p { margin:0; color:#667085; font-size:11px; }
-.admin-page .filter-bar { display:grid; grid-template-columns:minmax(220px,1fr) auto; gap:8px; align-items:center; margin-bottom:8px; }
 .search-box { position:relative; min-width:0; flex:1 1 220px; }
 .search-box > i { display:none; }
 .search-box input { padding-left:32px; padding-right:11px; }
@@ -468,7 +400,6 @@ html,body{min-width:0}body{line-height:1.6;-webkit-font-smoothing:antialiased;te
     .data-table-bordered-rows tbody td { padding:7px 8px; }
 }
 @media (max-width:560px) {
-    .admin-page .filter-bar,
     .admin-page .group-list { grid-template-columns:1fr; }
     .admin-page .selection-toolbar,
     .admin-page .panel-heading { align-items:stretch; flex-direction:column; }
diff --git a/src/YasPortal.Web/wwwroot/js/searchable-select.js b/src/YasPortal.Web/wwwroot/js/searchable-select.js
deleted file mode 100644
index fc8870e..0000000
--- a/src/YasPortal.Web/wwwroot/js/searchable-select.js
+++ /dev/null
@@ -1,205 +0,0 @@
-(() => {
-    const SELECTOR = 'select.form-control:not([data-searchable-disabled])';
-    const MIN_OPTIONS = 10;
-
-    function normalize(value) {
-        return (value || '').toLocaleLowerCase('fa-IR').trim();
-    }
-
-    function shouldEnhance(select) {
-        if (!select || select.multiple || select.dataset.searchableEnhanced === 'true') return false;
-        if (select.closest('.searchable-select')) return false;
-        return select.options.length >= MIN_OPTIONS;
-    }
-
-    function enhance(select) {
-        if (!shouldEnhance(select)) return;
-
-        const wrapper = document.createElement('div');
-        wrapper.className = 'searchable-select';
-        wrapper.setAttribute('dir', 'rtl');
-
-        select.parentNode.insertBefore(wrapper, select);
-        wrapper.appendChild(select);
-        select.dataset.searchableEnhanced = 'true';
-        select.classList.add('searchable-native-select');
-
-        const input = document.createElement('input');
-        input.type = 'text';
-        input.className = 'form-control searchable-select-input';
-        input.autocomplete = 'off';
-        input.setAttribute('role', 'combobox');
-        input.setAttribute('aria-autocomplete', 'list');
-        input.setAttribute('aria-expanded', 'false');
-        input.placeholder = select.options[0]?.textContent?.trim() || 'جستجو...';
-
-        const dropdown = document.createElement('div');
-        dropdown.className = 'searchable-select-dropdown';
-        dropdown.setAttribute('role', 'listbox');
-        dropdown.hidden = true;
-
-        wrapper.appendChild(input);
-        wrapper.appendChild(dropdown);
-
-        let activeIndex = -1;
-
-        function options() {
-            return Array.from(select.options)
-                .filter(option => option.value !== '')
-                .map(option => ({
-                    value: option.value,
-                    text: option.textContent.trim(),
-                    normalized: normalize(option.textContent)
-                }));
-        }
-
-        function selectedText() {
-            return select.selectedOptions.length && select.value
-                ? select.selectedOptions[0].textContent.trim()
-                : '';
-        }
-
-        function syncInput() {
-            input.value = selectedText();
-            input.disabled = select.disabled;
-            input.title = input.value;
-        }
-
-        function close() {
-            dropdown.hidden = true;
-            input.setAttribute('aria-expanded', 'false');
-            activeIndex = -1;
-        }
-
-        function choose(option) {
-            select.value = option.value;
-            select.dispatchEvent(new Event('change', { bubbles: true }));
-            syncInput();
-            close();
-        }
-
-        function render(filter = '') {
-            const query = normalize(filter);
-            const matches = options().filter(option => !query || option.normalized.includes(query));
-            dropdown.replaceChildren();
-            activeIndex = -1;
-
-            if (!matches.length) {
-                const empty = document.createElement('div');
-                empty.className = 'searchable-select-empty';
-                empty.textContent = 'موردی پیدا نشد';
-                dropdown.appendChild(empty);
-                return;
-            }
-
-            for (const option of matches.slice(0, 100)) {
-                const item = document.createElement('button');
-                item.type = 'button';
-                item.className = 'searchable-select-option';
-                item.textContent = option.text;
-                item.setAttribute('role', 'option');
-                item.dataset.value = option.value;
-                if (option.value === select.value) item.classList.add('selected');
-                item.addEventListener('mousedown', event => event.preventDefault());
-                item.addEventListener('click', () => choose(option));
-                dropdown.appendChild(item);
-            }
-
-            if (matches.length > 100) {
-                const more = document.createElement('div');
-                more.className = 'searchable-select-more';
-                more.textContent = `نتیجه‌های بیشتر با دقیق‌تر کردن جستجو نمایش داده می‌شوند (${matches.length} مورد)`;
-                dropdown.appendChild(more);
-            }
-        }
-
-        function open() {
-            if (input.disabled) return;
-            render(input.value === selectedText() ? '' : input.value);
-            dropdown.hidden = false;
-            input.setAttribute('aria-expanded', 'true');
-        }
-
-        input.addEventListener('focus', () => open());
-        input.addEventListener('input', () => {
-            if (input.value === selectedText()) {
-                open();
-                return;
-            }
-            render(input.value);
-            dropdown.hidden = false;
-            input.setAttribute('aria-expanded', 'true');
-        });
-
-        input.addEventListener('keydown', event => {
-            const items = Array.from(dropdown.querySelectorAll('.searchable-select-option'));
-            if (event.key === 'Escape') {
-                syncInput();
-                close();
-                return;
-            }
-            if (event.key === 'ArrowDown') {
-                event.preventDefault();
-                if (dropdown.hidden) open();
-                if (items.length) {
-                    activeIndex = Math.min(activeIndex + 1, items.length - 1);
-                    items.forEach((item, index) => item.classList.toggle('active', index === activeIndex));
-                    items[activeIndex]?.scrollIntoView({ block: 'nearest' });
-                }
-                return;
-            }
-            if (event.key === 'ArrowUp') {
-                event.preventDefault();
-                if (items.length) {
-                    activeIndex = Math.max(activeIndex - 1, 0);
-                    items.forEach((item, index) => item.classList.toggle('active', index === activeIndex));
-                    items[activeIndex]?.scrollIntoView({ block: 'nearest' });
-                }
-                return;
-            }
-            if (event.key === 'Enter') {
-                if (!dropdown.hidden && activeIndex >= 0 && items[activeIndex]) {
-                    event.preventDefault();
-                    const value = items[activeIndex].dataset.value;
-                    const option = options().find(item => item.value === value);
-                    if (option) choose(option);
-                }
-            }
-        });
-
-        input.addEventListener('blur', () => {
-            window.setTimeout(() => {
-                if (!wrapper.contains(document.activeElement)) {
-                    syncInput();
-                    close();
-                }
-            }, 120);
-        });
-
-        select.addEventListener('change', syncInput);
-        syncInput();
-    }
-
-    function scan(root = document) {
-        root.querySelectorAll?.(SELECTOR).forEach(enhance);
-    }
-
-    const observer = new MutationObserver(mutations => {
-        for (const mutation of mutations) {
-            mutation.addedNodes.forEach(node => {
-                if (node.nodeType === Node.ELEMENT_NODE) scan(node);
-            });
-        }
-    });
-
-    function start() {
-        scan();
-        observer.observe(document.body, { childList: true, subtree: true });
-    }
-
-    if (document.readyState === 'loading') {
-        document.addEventListener('DOMContentLoaded', start, { once: true });
-    } else {
-        start();
-    }
-})();
