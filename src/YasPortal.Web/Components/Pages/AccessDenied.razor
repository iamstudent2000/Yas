@page "/access-denied"

<PageTitle>Access denied - YasPortal</PageTitle>

<h1>Access denied</h1>
<p>You are authenticated, but your active position does not have the required permission.</p>
<p>Administrator access requires both <code>IsAdmin = true</code> and the requested permission on the active position.</p>
